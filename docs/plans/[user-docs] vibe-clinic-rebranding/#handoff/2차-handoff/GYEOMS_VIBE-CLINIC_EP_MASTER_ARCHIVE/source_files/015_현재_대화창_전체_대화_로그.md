# 015 — 현재 대화창 전체 대화 로그 (세션 서사 재구성)

> 형식: 시간순 사건 로그. 새 창에서 "무슨 일이 있었고 왜 그렇게 결정됐는가"를 즉시 복원하기 위한 것.
> 원문 축어록이 아니라 **결정·증거 중심의 완전 재구성**이다 (PDF 미생성 — 사유는 README 참조).
> 기간: 2026-07-15 ~ 2026-07-18 · 참여: 윤겸스 ↔ Claude Code (+병렬: Codex, Antigravity 세션)

---

## 막 1 — 1차 EP 인계와 C2 해소 (07-15)

- 1차 EP(EP_HANDOFF.json, SHA 5cfc6976…) 인계받아 상태 재검증: HEAD 8ea0443, 테스트 22/22, 병렬 커밋 3건(e01252f 하드닝·d575f9c 규칙보호·8ea0443 문서정합) diff 분석.
- C2 해소: 윤겸스 승인으로 **분리 커밋**(2.0.1 기능 02681e0 + 문서 33ede26).
- 병렬 세션 실시간 편집 감지 시작(ai-provider Gemini 단독화 등 — 문서 29~40이 의도 설명).

## 막 2 — 근본 로직 오류 사냥 (07-15)

- folder-picker `param()` 앞 인코딩 삽입 버그 수정(Base64 터널링 유지), 보안 회귀(Origin 'null' 허용) 차단, GET /api/errors 쓰기 부작용 제거, testCode 검증 추가. 커밋 d095462.
- **API 키 파괴 버그 규명**: 대시보드가 마스킹 키(12자)를 입력폼에 채워 저장 시 실키를 덮어씀 → 401 연쇄. saveByokConfig 가드 + 프론트 비채움. Diff 모달의 배열/객체 불일치(렌더 크래시), 로드실패 id 재매칭 결함까지 3중 사슬 수정. 커밋 2c8697b.
- 유출 직전 차단: 추적 중이던 example config.json에 실키 저장 상태 발견 → untrack + `**/.vibe-clinic/config.json` ignore (히스토리에는 마스킹 문자열뿐 — 실키 유출 없음 확증). 커밋 03df3d4.

## 막 3 — MIA 코어 P1~P5 (07-15, 문서 46 마스터플랜)

- P1+P3 (854eda4): Flaky Gate(확진/의심), triage.js 원인 후보 엔진, 회귀 게이트 + 자동 롤백 + 성숙도 라벨(VERIFIED_RESULT/ROLLED_BACK).
- P2 (8e3c8a3): 진단 약화 차단(BLOCKED_WEAKENING), 처방 전략 auto/local/ai, 처방 평가 메타.
- 수동 처방전 (7a2f451): 파일로 못 고치는 병(잔액0·네트워크)의 치료 경로 신설 — 진단 자체 prescription + 원인 기반 조치 가이드 + 재진단 버튼.
- P4+P5 (7d235d8): 치료 원장(treatment-ledger) + CURE_*.md 자동 축적, 확진/의심 배지·원인 후보·원장 타임라인 UI. IntersectionObserver 빈 카드 실패 모드 수정.
- 💉 치료 버튼: cureAll 계획(문서 52) → 병렬 세션이 구현·커밋(d8fba24~), '전체 치료'→'치료' 개명.
- 260714 별도 repo: C5 규칙(잔액0=WARNING)·한글화·prescription 탑재, 7b341bf까지 push.

## 막 4 — V2 재설계와 워크스페이스 대정리 (07-16)

- V2(React 재설계) handoff 무결성 검증: ZIP SHA `8763DD8B…F44A` 일치, snapshot 28/28. 붙여진 리뷰 10개 차단 항목을 **코드로 전수 재검증(메타검토)** — 전부 사실 + #1은 3중 결함으로 상향.
- Codex가 워크스페이스 정리(WORKSPACE_MAP.md·CLAUDE.md·support 통합·worktree 퇴역). 나는 대기 원칙 준수.

## 막 5 — 파티션 (07-17, 문서 57)

- 내 계획: 대안 C(논리 분리 선행 → 물리 분리 후행). Phase B 실행·커밋(65d3471): **shared/api-contract.md 단일 진실원**(16 엔드포인트 + V2 실측 함정 2개를 계약 조항화: 409 본문 버리지 말 것·마스킹 키 미회향), 진영 스크립트, 계약 테스트.
- 병렬 세션이 Phase C 물리 분리 + V2 통합 + 콘솔 통일 수행(9ec91a6~92564fb). V2 차단 항목 #1·#2·#5·#8·#10 해결 확인, #9(프론트 테스트 0) 잔존.

## 막 6 — "전 기능 불능" 사건 (07-17~18)

- 증상: v1·v2 화면은 뜨는데 모든 기능 무반응, 'Failed to fetch'.
- 실측 규명: **파티션 이전에 기동된 낡은 서버 프로세스**가 원인 — GET은 살고 POST에서 즉사(코드 파일이 발밑에서 이동). 현재 코드로 재기동 → 전 기능 정상. API 9종 + UI 8종 + 치료 E2E(ESM 프로젝트 완치 VERIFIED_RESULT) 실측 통과.
- 부수 결함 수정: dashboard:v1/v2·test:self 스크립트 `--cwd ..` 누락(빈 대시보드 원인), 자가진단 파일의 구 경로(../../src) 교정.
- 교훈 확정: 'Project: configured' 로그는 **의도된 프라이버시 규칙**(715e0db + 회귀 테스트) — 복원 시도를 철회하고 규칙 존중.

## 막 7 — 폴더 선택기 최종전 (07-18)

- 증상: [선택] 대화창이 **브라우저 뒤**에 뜸(첨부 스크린샷) + 구현 시도 중 500.
- 4차 수정(TopMost owner)·병렬 세션의 5번째 변형까지 **전부 실패**. 500의 정체 = 뒤에 떠서 안 보임 → 5분 타임아웃 kill.
- **아키텍처 벽 판정**: 백그라운드 서버 자식 프로세스는 포그라운드 불가(SetForegroundWindow 거부). 문서 58 §3에 실패 이력 5건 봉인.
- 윤겸스: "삭제하고 처음부터 다시 만드는 건 어때?" → MIA 분석 → **A안 승인**: OS 대화창 삭제 + 웹 내장 폴더 탐색기.
- 실행(계약 우선): api-contract에 `GET /api/fs/list` 선정의 + select 410 → 백엔드 구현(listDriveRoots/listDirectories, 읽기 전용) → picker 122줄 git rm → 테스트 교체(45/45) → V2 모달(드라이브→탐색→[이 폴더 선택]→changeProject) → 린트 0·빌드 → **브라우저 E2E 완주**: 모달이 대시보드 안에 표시, 상위/하위 탐색, 선택 확정 → 자동 진단 연쇄(진단 전 3→정상 3).
- **세션 종료 시점: 커밋 승인 질문에 응답 대기 중 (U1).**

## 마지막 상태 스냅샷

- HEAD 92564fb == origin/main · npm test 45/45 · 미커밋: 재건축 세트 전체 + docs 58~60 + dist 재빌드 + 사용자 소유 변경(dashboard.js 디버그 로그, legacy V1 App.jsx)
- 백그라운드 서버: 내 검증용(7701) 중지됨. 윤겸스 7700 서버는 구 코드 → 재시작 필요.
