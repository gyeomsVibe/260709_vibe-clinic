# MIA 실행 태스크

## Phase 1: 경로 인식 및 readBody 인코딩 오류 해결
- [x] `folder-picker.ps1` 최상단에 UTF-8 인코딩 명시 추가
- [x] `dashboard.js` 내 `readBody()`를 Buffer 수집 및 `Buffer.concat().toString('utf8')` 방식으로 교정
- [x] `folder-picker.ps1` 선택 경로를 UTF-8 바이트 -> Base64 문자열로 인코딩하여 출력
- [x] `dashboard.js` 내 `parseFolderPickerOutput()`에서 Base64 디코딩 디코더 탑재 (한글 경로 완전 무결 해결)

## Phase 2: AI 스캐너 행 방지 및 Gemini 전용화
- [x] `ai-provider.js` 내 fetch에 `AbortController` 25초 타임아웃으로 상향 조정 (지연 방지)
- [x] `ai-provider.js`에서 Gemini를 제외한 타 AI 제공자 제거
- [x] `dashboard.js` 내 `explainProject()`에서 429/503/aborted 에러 감지 시 즉시 로컬 폴백으로 전환
- [x] `dashboard.js` 내 프로젝트 요약 결과를 캐싱(`projectExplanationCache`)하여 불필요한 중복 호출 차단

## Phase 3: 진단 캔버스 제거 및 3열 카드 그리드 & 의사 소견 UI 개편
- [x] `dashboard.html`에서 진단 캔버스 마크업 및 `drawConnectors` 제거
- [x] `dashboard.html`에 `.cards-grid` 카드 그리드 마크업 신설 및 CSS 최적화
- [x] `dashboard.html` 내 BYOK 섹션에서 제공자 선택 삭제 및 Gemini 고정 라벨 처리
- [x] `createCard()` 및 `openSideDrawer()` 내 용어 한글화 및 "🩺 의사 소견" 구역 추가
- [x] 필터 툴바 버튼을 한글 상태 배지로 전환

## Phase 4: 로컬 파일 프로토콜 및 웹뷰 403 Origin 버그 해결
- [x] `dashboard.js` 내 `isAllowedDashboardOrigin()`에서 로컬 파일/웹뷰 Origin(`null`, `vscode-webview://`, `vscode-file://`, `.vscode-cdn.net`) 허용 처리
- [x] `dashboard.html`에서 `API` 상수를 브라우저 로딩 프로토콜(`file:`)일 때 `http://localhost:7700`으로 자동 폴백되도록 수정
- [x] `dashboard.html` 내 `runDiagnostics()`에서 서버가 반환하는 에러 메시지(`data.error`)를 toast 창에 정확하게 띄우도록 수정
- [x] `dashboard.html` 내 `runDiagnostics()` 및 `updateSummaryStats()`에서 기존 존재하지 않는 `statTotal` 계열 엘리먼트 널 포인터 참조 오류 해결

## Phase 5: GitHub 스타일 기술스택 바 구현 및 첨부3 완벽 싱크 & 기능 고도화
- [x] `dashboard.js`에서 프로젝트 파일 크기 분석을 통한 정확한 언어 비율 계산 함수 `calculateProjectLanguages` 구현
- [x] `dashboard.html` AI 프로젝트 스캐너 구역에 GitHub 스타일의 가로형 분포 게이지 바 및 레전드 추가
- [x] `dashboard.html` BYOK 입력 상자들을 접고 펼칠 수 있는 슬라이딩 토글 스위치 및 상세 사양 아코디언 구현
- [x] `dashboard.html` 진단 제어 툴바 버튼 디자인을 첨부3 이미지에 맞춘 5개 분할 약약 처방/필터 버튼으로 개편
- [x] `dashboard.html` 카드 그리드 최하단에 새 진단을 생성할 수 있는 점선 `+` 카드 렌더링 및 모달 팝업 추가
- [x] `/api/diagnostic/create` 신규 진단 파일 생성 API를 백엔드에 증설하고 프론트엔드 연동 완료
- [x] `dashboard.html` 헤더 영역에 "💡 사용 방법" 단추 이전 복원 완료
- [x] `dashboard.html` 헤더 우측 건강도 수치 텍스트가 CSS absolute 포지셔닝 때문에 하단에 둥둥 뜨던 버그 해결
- [x] `projectBar`와 `byokBar`를 단일 "환경 설정" 카드 패널로 결합하고 수직 배치 레이아웃 최적화 완료
- [x] `.vibe-clinic/error-patterns/` 하위에 현실적인 소수점 계산 오차(ERR_001) 및 0 나누기(ERR_002) 한글 에러 패턴 예시 자동 생성 유틸리티 구현 완료
- [x] `dashboard.html` 우측 메인 영역을 `정밀 진단 차트(1.6fr)`와 `검출된 오류 파일 패턴(1.1fr)`으로 가로 2열 배치 개편 (세로 빈 공간 낭비 완벽 해결)
- [x] `dashboard.js` 내 `generateLocalFallbackSummary` 고도화하여 AI 429 시에도 풍성하고 개별 매핑된 한글 정적 요약 피드백 출력 구현
- [x] 미초기화(`_no_diagnostics` 활성화) 상태 시 건강도를 `0%` 빨간 경고 대신 `대기` 회색 링으로 유도 렌더링하도록 교정
- [x] 긴 폴더 경로가 잘려 오인되는 것을 방지하도록 입력 창 툴팁 추가 및 한글 최종 폴더명을 향해 자동 우측 스크롤 탑재
- [x] 실패한 ID가 없는 상태 또는 미초기화 시에도 에러 패턴 목록을 항상 전부 노출하고, 실제 매핑된 경우에만 '감지됨' 배지 강조로 필터 조건 개선

## Phase 6: 중복 도움말 정리 및 초기화 용어 순화 & ES모듈 프로젝트 .cjs 로컬 처방 엔진 구축
- [x] 헤더 우측 영역에 중복 상주하던 물음표(`❓`) 아이콘 버튼 삭제 (💡 사용 방법 버튼만 유지)
- [x] 사용자 눈높이에 부합하도록 대시보드 전반에 노출되던 "초기화" 기술 용어를 **"진단 도구 설치 (초기화)"**로 순화 및 변경 완료
- [x] `package.json` 내 `"type": "module"` 선언 여부를 프로젝트 초기화 시 분석하여, ES 모듈 환경인 경우 기본 예제 진단 파일을 `example.clinic.cjs` 확장자로 자동 생성되도록 기능 고도화
- [x] `runner.js` 진단 스캐너가 `.clinic.js` 뿐만 아니라 `.clinic.cjs` 파일도 정상 스캔/로딩/구동할 수 있도록 정밀 개선 완료
- [x] AI API 호출 실패(Gemini 429 quota 에러 등) 상황 하에서도 즉시 로컬 치료 제안을 가동시키는 **로컬 처방 엔진(`generateLocalRepairProposal`)** 개발 및 백엔드 통합 완료
- [x] `ReferenceError: module is not defined in ES module scope` 로딩 에러 감지 시, `example.clinic.js` 파일을 즉시 `example.clinic.cjs`로 생성하고 기존 `.js` 파일을 삭제하도록 지시하는 무점검 자동 수리(Local Repair) 동작 탑재 완료
- [x] `npm test` CJS 로더 및 로컬 수리 폴백 검증 테스트 케이스 신규 작성 및 전체 25개 테스트 성공 통과 확인
