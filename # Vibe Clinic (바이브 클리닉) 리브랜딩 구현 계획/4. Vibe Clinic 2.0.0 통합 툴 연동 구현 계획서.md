# Vibe Clinic 2.0.0 통합 툴 연동 구현 계획서 (Codex 포함 보강본)

본 계획서는 `vibe-diagnosis` 프레임워크가 `Vibe Clinic` (버전 `2.0.0` hard-cut)으로 리브랜딩됨에 따라, 윤겸스님의 개발 환경 내 3가지 툴(Antigravity, Claude Code, Codex) 및 관련 설정들의 경로와 식별자를 정합화하기 위한 상세 실행 계획입니다.

## User Review Required

> [!IMPORTANT]
> - 본 패치는 로컬 사용자 디렉토리(`~/.gemini/config/`, `~/.claude/skills/`, `~/.codex/`) 및 Antigravity IDE 설정 파일에 직접 관여합니다.
> - 전역 MCP 설정(`mcp_config.json`)을 변경하므로, 패치 완료 후 Antigravity IDE 및 Claude Code의 MCP 연결 상태를 재확인해야 합니다.
> - Codex의 전역 세션 상태 파일(`.codex-global-state.json`)의 작업 폴더 경로를 갱신합니다.

## Proposed Changes

---

### 1. Antigravity IDE 전역 설정 정합화

#### [MODIFY] [mcp_config.json](file:///C:/Users/Kimyoongyeom/.gemini/config/mcp_config.json)
- 기존 `"vibe-diagnosis"`로 설정된 전역 MCP 서버 이름을 `"vibe-clinic"`으로 수정합니다.
- `args` 경로 내의 이전 리포지토리 폴더명인 `260709_vibe-diagnosis`를 `260709_vibe-clinic`으로 변경합니다.
  ```json
  "vibe-clinic": {
    "command": "node",
    "args": [
      "D:/D_Workspace_NB/-google-workspace/-antigravity-workspace/260709_vibe-clinic/mcp-server/index.js"
    ]
  }
  ```

#### [MODIFY] [AGENTS.md](file:///C:/Users/Kimyoongyeom/.gemini/config/AGENTS.md)
- 전역 에이전트 가이드 문서 내에 존재하는 구버전 용어와 명칭을 2.0.0 스펙에 맞춰 수정합니다:
  - `vibe-diagnosis` ➔ `vibe-clinic`
  - `.diag.js` ➔ `.clinic.js`
  - `run_diagnostics` ➔ `run_clinic`
  - `.vibe-diagnosis/` ➔ `.vibe-clinic/`

---

### 2. Codex 전역 경로 설정 정합화

#### [MODIFY] [.codex-global-state.json](file:///C:/Users/Kimyoongyeom/.codex/.codex-global-state.json)
- Codex가 인지하고 고정해 둔 기존 `260709_vibe-diagnosis` 물리 경로들을 새로운 리브랜딩 경로인 `260709_vibe-clinic`으로 일괄 변경합니다.
- 대상 키 항목:
  - `active-workspace-roots`
  - `pinned-project-ids`
  - `sidebar-collapsed-groups`
  - `electron-saved-workspace-roots`
  - `electron-workspace-root-labels`

---

### 3. Claude Code 로컬 권한 설정 및 매뉴얼 가이드 수정

#### [MODIFY] [.claude/settings.local.json](file:///d:/D_Workspace_NB/-google-workspace/-antigravity-workspace/260709_vibe-clinic/.claude/settings.local.json)
- Claude Code의 자동 실행 권한 목록 내에 들어 있는 구버전 경로 `260709_vibe-diagnosis`를 `260709_vibe-clinic`으로 일괄 치환합니다.
- 예전 CLI 파일명인 `vibe-diag.js` 관련 권한 항목을 새 규격인 `vibe-clinic.js` 및 `vbc` 명령어로 업데이트합니다.

#### [MODIFY] [claude_vibe_check_mode/implementation_plan.md](file:///d:/D_Workspace_NB/-google-workspace/-antigravity-workspace/260709_vibe-clinic/claude_vibe_check_mode/implementation_plan.md)
- 매뉴얼에 기재된 로컬 MCP 서버 등록 가이드 명령어 내 `vibe-diagnosis` 명칭 및 경로를 `vibe-clinic`으로 갱신합니다.
- `sync-rules.js` 경로를 `bin/vibe-clinic.js` / `bin/sync-rules.js` 등으로 맞춥니다.

---

### 4. 내부 소스 및 테스트 케이스 정합화

#### [MODIFY] [test/unit.test.js](file:///d:/D_Workspace_NB/-google-workspace/-antigravity-workspace/260709_vibe-clinic/test/unit.test.js)
- 107라인 부근의 기존 디렉토리 제거 확인 테스트에서 `.vibe-diagnosis` 디렉토리 체크 부분을 새로운 표준인 `.vibe-clinic` 디렉토리 제거 체크로 수정합니다.

---

### 5. 규칙 및 스킬 동기화 강제 기동
- 패치 적용 후 `npm run sync:rules -- --global` 스크립트를 수동 기동하여, 변경된 `GEMINI.md` 규칙 본문이 Claude 글로벌 스킬 디렉토리(`C:\Users\Kimyoongyeom\.claude\skills\vibe-check\SKILL.md`)에 강제로 동기화되도록 조치합니다.

---

## Verification Plan

### Automated Tests
- `npm run test` 를 기동하여 수정된 단위 테스트가 모두 정상 통과(11/11)하는지 검증합니다.
- `npm run test:self` 를 기동하여 리브랜딩된 로컬 자가진단 노드가 100% 정상 작동하는지 확인합니다.

### Manual Verification
- Antigravity IDE 및 Codex를 재실행하거나 MCP 서버를 새로고침하여 전역 등록된 `vibe-clinic` MCP 도구(`run_clinic`, `init_clinic`, `list_clinics`)들이 정상적으로 로드 및 감지되는지 확인합니다.
- Codex 툴을 켜고 리브랜딩된 워크스페이스 명칭(`260709_vibe-clinic`)이 정상적으로 감지 및 핀 고정 해제/유지되는지 검증합니다.
