MIA 모드 — 원인 파악부터. 증상: 260714의 "주의 관찰"(잔액 0 WARNING)에서 처방 요청을 해도 아무 결말이 없음. 먼저 실서버로 정확히 어디서 끊기는지 재현합니다.

repairer에 수동 처방전 생성기 + 폴백 배선:

dashboard 핸들러 — MANUAL 처방은 적용 대상이 없으므로 저장 없이 즉시 반환:

UI — 수동 처방전 모달 추가:

테스트 2건 추가:

윤겸스, "치료 버튼-로직이 없다"는 관찰이 정확했고 — **그 빠진 로직을 신설해 치료 경로를 완성**했습니다.

---

## ✅ 근본 원인 파악 및 치료 완료 보고

### 1. 실행한 것 / 2. 발견한 것
**원인 2겹을 실측으로 확정:**
- **표면**: 260714에 API 키가 실제로는 저장돼 있지 않아 처방 요청이 7ms 만에 실패 (토스트만 스쳐 지나가 "변화 없음"으로 체감)
- **근본**: 이 "주의 관찰"(SUT 잔액 0)은 **파일 수정으로 고칠 수 없는 병**입니다. 치료 = "지갑에 토큰 입금"이라는 운영 조치인데, 기존 파이프라인엔 **코드 수정형 처방만 존재** — 키가 있어도 AI는 고칠 파일이 없거나 진단 약화 가드에 차단돼 어차피 막다른 길이었습니다. 말씀하신 "③ 치료 버튼-로직 부재"의 정체입니다.

**신설한 치료 경로 — 수동 처방전(행동 처방):**
- 진단 파일이 `prescription` 필드로 **스스로 조치법을 제공** 가능 ([runner.js](src/runner.js) 전달) — 260714 진단에 실제 처방 탑재(지갑 주소 포함 입금 안내 3단계)
- [repairer.js](src/repairer.js) `generateManualPrescription`: 진단 자체 처방 → 없으면 triage 원인 기반 조치 가이드(네트워크/쿼터·타임아웃·모듈 미설치). **모든 막다른 길에 폴백 배선**: 로컬 룰 없음·AI가 "파일로 못 고침"·약화 차단·API 실패
- UI: **📋 수동 처방전 모달** — 번호 매긴 조치 단계 + **[🩺 조치 완료 — 재진단 실행]** 버튼 (이것이 새 "치료 버튼")

### 3. 수정한 것
- 본 저장소 커밋 `7a2f451` (8개 파일), 260714 커밋 `7b341bf`

### 4. 다시 실행한 검증
- `npm test` → **38/38 통과** (신규 2건: 진단 자체 처방 → MANUAL 발급, 네트워크 원인 → 조치 가이드 생성)
- **260714 실서버 E2E**: 잔액 0 WARNING에 처방 요청 → `kind:"MANUAL"` + **3단계 처방전**(1단계: "매니저 지갑(0x7660…)에 SUT 토큰을 입금하세요") — **키 없이도 작동 확인**

### 5. 아직 실행하지 않은 것
- 대시보드에서 직접 확인: 260714 프로젝트 → 주의 관찰 카드 → "AI 치료 처방 요청" → 처방전 모달이 뜹니다 (서버 재시작 필요 — 기존 실행 중인 대시보드는 구 코드입니다)
- **AI 코드 수리용 키 재등록**: 260714에서 수리형 치료까지 쓰려면 키를 한 번 저장해 주세요 (수동 처방전은 키 불필요)
- 마스터플랜 잔여: P4(치료 원장), P5(UI 배지·전략 선택)

### 6. 다음 승인 필요 항목
- **push 2건**: 본 저장소 `8e3c8a3`+`7a2f451`, 260714 저장소 커밋 3건(`8a9770f`·`110f789`·`7b341bf`)
- P4 착수 여부